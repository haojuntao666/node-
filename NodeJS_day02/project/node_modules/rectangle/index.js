/**
 * ES6之前的语法：
 * btn.onclick = function(e){}
 *
 * ES6中创建匿名函数的语法：(箭头函数)
 * btn.onclick = (e)=>{ }
 *
 */

module.exports.size = (w, h)=> {
  return w * h;
}

module.exports.perimeter = (w, h)=> {
  return 2 * (w + h);
}




